/*
 * @method field
 *
 * */
;(function(root, Checkers) {
  'use strict';
  var attributes = {
    x: 8,
    y: 8
  };

  Checkers.methods.Field = function (options) {

    return {
      mapper: function () {
      
      },
      render: function () {
      
      }
    }
  }

} (window, window.Checkers));
